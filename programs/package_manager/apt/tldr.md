```
# search for a package but do not match text `fzf` to package description
# but to package names only
apt search --names-only fzf

# install specific version 0.16.1-1 of gparted
sudo apt-get install gparted=0.16.1-1


# list installed and in the repository available version of gparted
apt-cache policy gparted











```
